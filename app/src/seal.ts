/** Seal encrypt/decrypt via on-chain policy. */

export async function encrypt(_plaintext: Uint8Array): Promise<Uint8Array> {
  // TODO: encrypt using Seal policy
  return new Uint8Array();
}

export async function decrypt(_ciphertext: Uint8Array): Promise<Uint8Array> {
  // TODO: decrypt using Seal policy
  return new Uint8Array();
}
